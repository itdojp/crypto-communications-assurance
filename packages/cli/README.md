# CLI package placeholder

A future local CLI may expose deterministic contract validation. No CLI is
implemented in BOOT-001, and this directory does not authorize network calls,
scanner execution, live-target access, release, or publication.
