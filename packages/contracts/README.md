# Contracts package

This private bootstrap package contains a small AJV Draft 2020-12 compilation
helper. It performs local validation only and conveys no human approval,
compatibility guarantee, or release authority.

The package is not published during bootstrap.
